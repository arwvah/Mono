package com.mono.fitness.data

/**
 * Seed data removed to prioritize user-generated content.
 */
object SeedData {
    suspend fun seedIfEmpty(repo: MonoRepository) {
        // No seed data added.
    }
}
